node_modules
dist
build
.turbo
.next
.expo
*.tsbuildinfo
.env
.env.*
!.env.example
coverage
.DS_Store
*.log

# Storybook build output
**/storybook-static/

# Playwright MCP temp
.playwright-mcp/

# graphify outputs + chats
graphify-out/
.graphifyignore
.chats/

# Temporary scheduled task locks
.claude/scheduled_tasks.lock

# Kit-local TODO tracker
kits/*/TODO.md

# Railway deploy script byproduct (rebuild marker)
.build-timestamp

# Phase 12 · E4 — SEO audit snapshots are now stored in Postgres.
# This dir was used by an earlier JSON-file design — keep ignored in case
# any local script writes there as a fallback.
data/seo-snapshots/
