import { ShowcaseFrame, ComingSoon } from '../../components/ShowcaseFrame'

export default function StepPageLayoutShowcase() {
  return (
    <ShowcaseFrame
      title="Step Page Layout"
      description="Onboarding-step shell with progress + footer CTA."
      docPath="packages/ui-native/src/layouts/StepPageLayout.tsx"
    >
      <ComingSoon title="Stub — wire up next milestone" />
    </ShowcaseFrame>
  )
}
