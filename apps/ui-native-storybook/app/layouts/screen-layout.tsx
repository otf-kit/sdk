import { ShowcaseFrame, ComingSoon } from '../../components/ShowcaseFrame'

export default function ScreenLayoutShowcase() {
  return (
    <ShowcaseFrame
      title="Screen Layout"
      description="Padded scrollable shell every screen sits inside."
      docPath="packages/ui-native/src/layouts/ScreenLayout.tsx"
    >
      <ComingSoon title="Stub — wire up next milestone" />
    </ShowcaseFrame>
  )
}
