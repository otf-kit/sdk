import { ShowcaseFrame, ComingSoon } from '../../components/ShowcaseFrame'

export default function PaywallScreenShowcase() {
  return (
    <ShowcaseFrame
      title="Paywall Screen"
      description="Subscription wall with hero + plans."
      docPath="packages/ui-native/src/patterns/PaywallScreen.tsx"
    >
      <ComingSoon title="Stub — wire up next milestone" />
    </ShowcaseFrame>
  )
}
