import { ShowcaseFrame, ComingSoon } from '../../components/ShowcaseFrame'

export default function ProfileHeaderShowcase() {
  return (
    <ShowcaseFrame
      title="Profile Header"
      description="Avatar + name + meta + actions."
      docPath="packages/ui-native/src/patterns/ProfileHeader.tsx"
    >
      <ComingSoon title="Stub — wire up next milestone" />
    </ShowcaseFrame>
  )
}
