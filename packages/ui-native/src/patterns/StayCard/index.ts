// Real-estate / stay marketing screens — premium-feel patterns for
// property listings + booking flows. Standalone with sensible defaults
// so they read as polished marketing screenshots out of the box, but
// every label / image / CTA is prop-overridable for real product use.
//
// Pair with `<Shockwave>` for the chromatic-ripple transition between
// browse + detail (see apps/ui-native-showcase/components/shockwave-demo).
export { StayBrowseScreen } from './StayBrowseScreen'
export { StayDetailScreen } from './StayDetailScreen'
export type {
  StayBrowseScreenProps,
  StayCategoryChip,
  StayDetailMetric,
  StayDetailScreenProps,
  StayListing,
} from './types'
